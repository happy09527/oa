<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.auth.mapper.SysMenuMapper">

    <select id="findMenuListByUserId" resultType="com.example.model.system.SysMenu">
--         select * from sys_menu where is_deleted=0 and status =1  and id in (
--             select menu_id from sys_role_menu where is_deleted=0 and role_id in (
--                 select role_id from sys_user_role where is_deleted=0 and user_id=#{userId}
--             )
--         );
        select
        distinct  m.id,m.parent_id,m.name,m.type,m.path,m.component,m.perms,m.icon,m.sort_value,m.status,m.create_time,m.update_time,m.is_deleted
        from sys_menu m
        inner join sys_role_menu rm on rm.menu_id = m.id
        inner join sys_user_role ur on ur.role_id = rm.role_id
        where
        ur.user_id = #{userId}
        and m.status = 1
        and rm.is_deleted = 0
        and ur.is_deleted = 0
        and m.is_deleted = 0
    </select>
</mapper>
